use lib::Number;
use lib::Add;
use lib::Multiply;
use lib::Element;

pub mod lib;

fn main() {
    //let i = Number::new(3);
    //println!("{}", i)
    //println!("red? {}", i.is_reducible());

    //let i = Add::new(Number::new(3), Number::new(4));
    //println!("{}", i)
    //println!("red? {}", i.is_reducible());

    //let i = Multiply::new(
        //Add::new(Number::new(3), Number::new(4)),
        //Number::new(2));
    //println!("{}", i)
    //println!("red? {}", i.is_reducible());
}
